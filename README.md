Simple program to explore the basics of the `asyncio` API for a training.

* `generate.py` generates ~5GBs of random log files in `target`
* `main.py`     parses them into a structured format in sqlite
